name: Codeowners merging

on:
  pull_request_target:
    types: [opened]
  issue_comment:
    types: [created]
  pull_request_review:
    types: [submitted]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      statuses: read

    steps:
      # 🤖 Langkah Generator Token Bot Pribadi (thekingtermux-bot)
      - name: Generate Bot Token
        id: bot_token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.BOT_APP_ID }}
          private-key: ${{ secrets.BOT_PRIVATE_KEY }}

      - uses: actions/checkout@v6

      - name: Run Codeowners merge check
        uses: OSS-Docs-Tools/code-owner-self-merge@1.6.8
        env:
          # Memaksa action menggunakan token bot pribadi yang memiliki hak akses penuh
          GITHUB_TOKEN: ${{ steps.bot_token.outputs.token }}
        with:
          path: '.github/CODEOWNERS'
